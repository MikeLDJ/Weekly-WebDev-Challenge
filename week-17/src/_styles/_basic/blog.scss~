// blog

.blog {
  @include flex(center);
  flex-direction: column;
  height: 845px;
}

.blog-info {
  @include container;
  height: 445px;

  h4 {
    margin: 2.7em 0 .5em -0.3em;
    text-transform: capitalize;

    span {
      margin-left: .35em;
    }
  }

  p {
    margin-top: .9em;
  }
}

.blog-promo {
  @include flex(center);
  padding-bottom: 110px;

  .view-promo {
    @include button;
    // background-color: #f00;
    height: 21.88em;
    margin: 1.15em;
    width: 17.81em;

    h5 {
      background-color: grey;
      color: $pipe;
      margin: 0;
      height: 120px;
      padding-top: 1em;
      border: solid 1px $font-light;
      border-color: transparent transparent $font-light transparent;
      border-radius: 50%/250px 0 250px 0;


      span {
        color: $font-blog;
      }
    }

    button {
      display: block;
      height: 2.6em;
      margin: 2em auto;
      width: 6em;
    }
  }
}
